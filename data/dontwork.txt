heredoc leak,
fds not closed in heredoc, (done in heredoc)
dont expand $... in heredoc limiter (but still expand inside the heredoc)
leaks when command not found -> miaou > {|}|}{|}D {AS|}D {A|S}D{ ASD|}A{SD}|A{SD|}{W}|DA|S}DAWD{AS|}D{ W|} leaks 
handle export without arguments
handle export +=
exports should only start with alphabetical characters (and rest is alphanum char)
unset path + cmd gives invalid reads and leaks
return exit code when exit (keep in unsigned char to handle overflows)
handle exit with more than 2 args
handle rm -rf when inside the directory you want to remove
	> cd && clear && mkdir test99 && cd test99 && rm -rf ../test99
	> cd && clear && mkdir test89 && cd test89 && rm -rf ../test89